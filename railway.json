{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd gifting-backend && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd gifting-backend && npx prisma migrate deploy && npm start",
    "healthcheckPath": "/api/products",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
